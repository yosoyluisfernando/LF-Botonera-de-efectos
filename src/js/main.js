/**
 * Archivo: main.js
 * Propósito: Punto de entrada mínimo. El contrato de arranque vive en startup.js.
 */

import { startApp } from './startup.js';

document.addEventListener('DOMContentLoaded', startApp);
